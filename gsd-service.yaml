apiVersion: v1
kind: Service
metadata:
  name: getting-started
  app: getting-started
spec:
  selector:
    app.kubernetes.io/name: getting-started
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000